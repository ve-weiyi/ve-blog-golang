syntax = "v1"

import "base.api"
import "types.api"

@server(
    prefix: /api/v1
    group: upload
)

service blog-api {
    @doc "上传文件"
    @handler UploadFile
    post /upload/:label () returns (UploadRecord)

@doc "上传语言"
    @handler UploadVoice
    post /voice () returns (UploadRecord)
}
