volumes:
  redis:

services:
  redis:
    image: redis:alpine
    container_name: redis-server
    restart: always
    ports:
      - '6379:6379'
    environment:
      - REDIS_PASSWORD=redis7914
    volumes:
      - redis:/data
